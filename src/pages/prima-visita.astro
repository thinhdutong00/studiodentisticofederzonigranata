---
import BaseLayout from '../components/BaseLayout.astro';
import { site } from '../data/site';

const pageDescription =
  'Prima visita odontoiatrica a Modena e Reggio Emilia: diagnosi chiara, priorità, tempi e costi prima di iniziare.';

const firstVisitFormPath = '/prenota-prima-visita/';

const visitSteps = [
  {
    title: 'La tua storia clinica',
    text: 'Raccogliamo informazioni su salute, farmaci, allergie e cure precedenti.',
  },
  {
    title: 'Ascolto e aspettative',
    text: 'Ascoltiamo il problema, i dubbi e il risultato che vorresti ottenere.',
  },
  {
    title: 'Controllo orale',
    text: 'Controlliamo denti, gengive e tessuti. Se serve, indichiamo gli esami utili.',
  },
  {
    title: 'Valutazione e piano di cura',
    text: 'Ti spieghiamo cosa abbiamo rilevato, le priorità e le possibili cure.',
  },
];

const evaluationIcons = {
  mainProblem: `
    <circle cx="21" cy="21" r="10" />
    <path d="m28.5 28.5 8 8" />
    <path d="M18.3 18.5a3.1 3.1 0 0 1 6 1.3c0 2.4-3.3 2.7-3.3 5" />
    <circle cx="21" cy="28.5" r="1" fill="currentColor" stroke="none" />
  `,
  teethAndGums: `
    <path d="M17.5 8.5c2.7-1.2 4.8.4 6.5 1.1 1.7-.7 3.8-2.3 6.5-1.1 5.5 2.4 6.2 9.8 3.1 15.4-1.3 2.4-2.1 4.8-2.5 7.5-.7 4.4-2.2 8.1-5 8.1-1.7 0-2.3-2.3-2.7-4.8-.3-2.1-.8-4.1-1.4-4.1s-1.1 2-1.4 4.1c-.4 2.5-1 4.8-2.7 4.8-2.8 0-4.3-3.7-5-8.1-.4-2.7-1.2-5.1-2.5-7.5-3.1-5.6-2.4-13 3.1-15.4Z" />
  `,
  exams: `
    <rect x="9" y="9" width="30" height="30" rx="4" />
    <path d="M15 21v-6h6M27 15h6v6M33 27v6h-6M21 33h-6v-6" />
    <circle cx="24" cy="24" r="4" />
  `,
  treatmentOptions: `
    <path d="M10 24h11" />
    <path d="M21 24c5 0 5-9 10-9h6" />
    <path d="M21 24c5 0 5 9 10 9h6" />
    <path d="m33 11 4 4-4 4M33 29l4 4-4 4" />
  `,
  priorities: `
    <circle cx="24" cy="26" r="12" />
    <path d="M24 19v7l5 3M18 9h12M24 9v5" />
  `,
  quote: `
    <path d="M12 8h24v32l-4-2.5-4 2.5-4-2.5-4 2.5-4-2.5-4 2.5V8Z" />
    <path d="M18 16h12M18 22h12M26 29h5M26 34h5" />
    <circle cx="19" cy="31.5" r="3.5" />
  `,
};

const evaluationAreas = [
  {
    icon: evaluationIcons.mainProblem,
    label: 'Il problema principale',
    text: 'Da dove nasce il disturbo, il dubbio o il desiderio che ti porta in studio.',
  },
  {
    icon: evaluationIcons.teethAndGums,
    label: 'Denti e gengive',
    text: 'Carie, vecchie cure, fratture, infiammazione, sanguinamento e stabilità.',
  },
  {
    icon: evaluationIcons.exams,
    label: 'Radiografie ed esami',
    text: 'Valutiamo ciò che hai già. Se serve altro, lo indichiamo in modo mirato.',
  },
  {
    icon: evaluationIcons.treatmentOptions,
    label: 'Opzioni di cura',
    text: 'Quando ci sono più strade, spieghiamo vantaggi, limiti e senso clinico.',
  },
  {
    icon: evaluationIcons.priorities,
    label: 'Priorità e tempi',
    text: 'Non tutto va fatto subito. Distinguiamo urgenze, priorità e controlli.',
  },
  {
    icon: evaluationIcons.quote,
    label: 'Preventivo',
    text: 'Il piano viene collegato a tempi e costi, prima della decisione.',
  },
];

const preparationItems = [
  'Radiografie, TAC, referti o preventivi già ricevuti.',
  'Farmaci assunti e allergie note.',
  'Bite, apparecchi, mascherine o protesi.',
  'Domande, dubbi, paure o obiettivi estetici.',
];

const reasonIcons = {
  toothPain: `
    <path d="M17.5 8.5c2.7-1.2 4.8.4 6.5 1.1 1.7-.7 3.8-2.3 6.5-1.1 5.5 2.4 6.2 9.8 3.1 15.4-1.3 2.4-2.1 4.8-2.5 7.5-.7 4.4-2.2 8.1-5 8.1-1.7 0-2.3-2.3-2.7-4.8-.3-2.1-.8-4.1-1.4-4.1s-1.1 2-1.4 4.1c-.4 2.5-1 4.8-2.7 4.8-2.8 0-4.3-3.7-5-8.1-.4-2.7-1.2-5.1-2.5-7.5-3.1-5.6-2.4-13 3.1-15.4Z" />
    <path d="M32 12.5 28.5 18H34l-4 6.5" />
  `,
  bleedingGums: `
    <path d="M24 10.5c3.5 4.3 5.2 7.2 5.2 9.8a5.2 5.2 0 0 1-10.4 0c0-2.6 1.7-5.5 5.2-9.8Z" />
    <path d="M12.5 28c3.2-3.8 7-5.7 11.5-5.7S32.3 24.2 35.5 28" />
    <path d="M14 30.5c2.3 4.1 5.7 6.2 10 6.2s7.7-2.1 10-6.2" />
  `,
  implant: `
    <path d="M18 8.5h12a4.5 4.5 0 0 1 4.5 4.5v2.5A6.5 6.5 0 0 1 28 22h-8a6.5 6.5 0 0 1-6.5-6.5V13A4.5 4.5 0 0 1 18 8.5Z" />
    <path d="M24 22v18" />
    <path d="M19 27h10" />
    <path d="M20 32h8" />
    <path d="M21 37h6" />
  `,
  orthodontics: `
    <path d="M10.5 20.5c3.3-5.6 7.8-8.4 13.5-8.4s10.2 2.8 13.5 8.4" />
    <path d="M12 26h24" />
    <rect x="13" y="22" width="6" height="8" rx="2" />
    <rect x="21" y="22" width="6" height="8" rx="2" />
    <rect x="29" y="22" width="6" height="8" rx="2" />
    <path d="M14.5 34c2.9 2.2 6.1 3.3 9.5 3.3s6.6-1.1 9.5-3.3" />
  `,
  aesthetics: `
    <path d="M12.5 28.5c3.5 5.8 7.3 8.7 11.5 8.7s8-2.9 11.5-8.7" />
    <path d="M15 20.5c2.4-5.1 5.4-7.7 9-7.7s6.6 2.6 9 7.7" />
    <path d="M24 13.5v20" />
    <path d="M18.8 16.5 17.5 31" />
    <path d="m29.2 16.5 1.3 14.5" />
    <path d="m38 8.5 1.2 3.1 3.3 1.2-3.3 1.3-1.2 3.1-1.2-3.1-3.3-1.3 3.3-1.2L38 8.5Z" />
    <path d="m10 12.5.8 2.1 2.2.9-2.2.8-.8 2.2-.8-2.2-2.2-.8 2.2-.9.8-2.1Z" />
  `,
  checkup: `
    <rect x="13" y="10" width="22" height="30" rx="4" />
    <path d="M20 10V8h8v2" />
    <path d="m18 25 4 4 8-9" />
    <path d="M18 34h12" />
  `,
  calendar: `
    <rect x="10" y="11" width="28" height="27" rx="4" />
    <path d="M16 8v6" />
    <path d="M32 8v6" />
    <path d="M10 19h28" />
    <circle cx="24" cy="31" r="7" />
    <path d="M24 27.5V31l3 1.8" />
  `,
  secondOpinion: `
    <path d="M12 13h18a5 5 0 0 1 5 5v7a5 5 0 0 1-5 5h-8.5L14 36v-6h-2a5 5 0 0 1-5-5v-7a5 5 0 0 1 5-5Z" />
    <path d="M18 20h11" />
    <path d="M18 25h7" />
    <circle cx="33.5" cy="32.5" r="5.5" />
    <path d="m37.5 36.5 3.5 3.5" />
  `,
};

const goodReasons = [
  {
    icon: reasonIcons.toothPain,
    title: 'Mi fa male un dente',
    text: 'Capire l’origine del dolore aiuta a decidere subito cosa fare.',
  },
  {
    icon: reasonIcons.bleedingGums,
    title: 'Ho le gengive che sanguinano',
    text: 'Il sanguinamento è un segnale. Va letto, non normalizzato.',
  },
  {
    icon: reasonIcons.implant,
    title: 'Voglio sapere se mi servono impianti',
    text: 'Valutiamo denti mancanti, osso, gengive e alternative possibili.',
  },
  {
    icon: reasonIcons.orthodontics,
    title: 'Vorrei raddrizzare i denti',
    text: 'Allineatori e apparecchi partono da diagnosi e obiettivi realistici.',
  },
  {
    icon: reasonIcons.aesthetics,
    title: 'Mi interessano faccette o estetica',
    text: 'Prima dell’estetica guardiamo salute, funzione e aspettative.',
  },
  {
    icon: reasonIcons.checkup,
    title: 'Voglio un controllo generale',
    text: 'Anche senza dolore, un controllo può intercettare problemi iniziali.',
  },
  {
    icon: reasonIcons.calendar,
    title: 'Non vado dal dentista da anni',
    text: 'Si riparte senza giudizio. Una priorità alla volta.',
  },
  {
    icon: reasonIcons.secondOpinion,
    title: 'Ho un preventivo e voglio un secondo parere',
    text: 'Rileggiamo diagnosi, opzioni, tempi e senso del piano proposto.',
  },
];

const offices = site.offices.map((office) => ({
  ...office,
  mapHref: `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(office.address)}`,
}));
---

<BaseLayout
  title={`Prima visita | ${site.name}`}
  description={pageDescription}
  ogImage="/assets/original/2022/03/dentista-modena-reggio-emilia.jpg"
  canonicalPath="/prima-visita/"
  transparentHeader
  headerCtaHref={firstVisitFormPath}
>
  <div class="first-visit-page">
    <section class="fv-hero" aria-labelledby="first-visit-title">
      <div class="fv-hero-panel" data-transparent-header-region>
        <div class="fv-wrap fv-hero-inner">
          <div class="fv-hero-copy">
            <p class="fv-kicker">Prima visita odontoiatrica</p>
            <h1 id="first-visit-title">Prima visita</h1>
            <p>
              Dolore, gengive, impianti, denti storti, estetica, controllo o secondo parere.
              Ti diciamo cosa c’è, cosa serve e cosa aspettarti prima di iniziare.
            </p>
          </div>
        </div>
      </div>
      <figure class="fv-hero-media">
        <picture>
          <source media="(max-width: 720px)" srcset="/assets/original/2026/prima-visita-studio-mobile.png" />
          <img src="/assets/original/2022/03/dentista-modena-reggio-emilia.jpg" alt="Sala operativa dello Studio Dentistico Federzoni Granata" loading="eager" />
        </picture>
      </figure>
    </section>

    <section id="come-funziona" class="fv-steps-section" aria-labelledby="steps-title">
      <div class="fv-wrap">
        <div class="fv-section-head fv-section-head-right">
          <p class="fv-kicker">Come si svolge</p>
          <h2 id="steps-title">La tua prima visita</h2>
        </div>

        <div class="fv-step-rail">
          {visitSteps.map((step, index) => (
            <article class="fv-step-card">
              <span class="fv-step-number">{index + 1}.</span>
              <div class="fv-step-body">
                <h3>{step.title}</h3>
                <p>{step.text}</p>
              </div>
            </article>
          ))}
        </div>

        <div class="fv-step-cta">
          <a class="fv-button fv-button-primary" href={firstVisitFormPath}>Richiedi informazioni <span aria-hidden="true">↗</span></a>
        </div>
      </div>
    </section>

    <section class="fv-split fv-split-soft" aria-labelledby="diagnosis-title">
      <div class="fv-wrap fv-split-grid">
        <figure class="fv-split-image">
          <img src="/assets/feature-stack/tecnologia-studio.jpg" alt="Tecnologia digitale per la valutazione odontoiatrica" loading="eager" />
        </figure>
        <div class="fv-split-copy">
          <p class="fv-kicker">Diagnosi prima del piano</p>
          <h2 id="diagnosis-title">Prima capiamo il caso, poi costruiamo il percorso</h2>
          <p>
            Una buona visita trasforma un problema confuso in informazioni utili: diagnosi, urgenza, opzioni, esami necessari.
          </p>
          <p>
            Se il percorso richiede più passaggi, lo diciamo. Prima si capisce, poi si decide.
          </p>
          <a class="fv-inline-link" href="/trattamenti/">Scopri i trattamenti <span aria-hidden="true">↗</span></a>
        </div>
      </div>
    </section>

    <section class="fv-evaluation" aria-labelledby="evaluation-title">
      <div class="fv-wrap">
        <div class="fv-section-head fv-section-head-centered">
          <p class="fv-kicker">Cosa valutiamo</p>
          <h2 id="evaluation-title">La visita serve a mettere ordine</h2>
          <p>
            Dal sintomo al piano. Dal dubbio alla scelta. Senza improvvisare.
          </p>
        </div>

        <div class="fv-evaluation-grid">
          {evaluationAreas.map((area) => (
            <article class="fv-evaluation-card">
              <span class="fv-evaluation-icon" aria-hidden="true">
                <svg viewBox="0 0 48 48" aria-hidden="true" role="presentation" focusable="false" set:html={area.icon} />
              </span>
              <div class="fv-evaluation-copy">
                <strong>{area.label}</strong>
                <p>{area.text}</p>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="fv-trust-band" aria-labelledby="trust-title">
      <div class="fv-wrap fv-trust-grid">
        <div>
          <p class="fv-kicker">Un approccio chiaro</p>
          <h2 id="trust-title">Non serve aspettare il dolore</h2>
        </div>
        <div class="fv-trust-copy">
          <p>
            Molti problemi dentali restano silenziosi: carie iniziali, gengive infiammate, vecchie otturazioni, denti instabili.
          </p>
          <p>
            Controllare non significa curare tutto. Significa sapere cosa osservare e cosa affrontare.
          </p>
          <a class="fv-button fv-trust-cta" href={firstVisitFormPath}>Prenota una visita <span aria-hidden="true">↗</span></a>
        </div>
      </div>
    </section>

    <section class="fv-reasons-section" aria-labelledby="reasons-title">
      <div class="fv-wrap">
        <div class="fv-section-head fv-section-head-right">
          <p class="fv-kicker">Quando prenotarla</p>
          <h2 id="reasons-title">Qual è il tuo motivo?</h2>
        </div>

        <div class="fv-reasons-grid">
          {goodReasons.map((reason) => (
            <article class="fv-reason-card">
              <span class="fv-reason-icon" aria-hidden="true">
                <svg viewBox="0 0 48 48" aria-hidden="true" role="presentation" focusable="false" set:html={reason.icon} />
              </span>
              <div class="fv-reason-copy">
                <h3>{reason.title}</h3>
                <p>{reason.text}</p>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="fv-split" aria-labelledby="prepare-title">
      <div class="fv-wrap fv-split-grid fv-split-grid-reverse">
        <div class="fv-split-copy">
          <p class="fv-kicker">Prima dell'appuntamento</p>
          <h2 id="prepare-title">Cosa portare alla prima visita</h2>
          <p>
            Se hai documenti recenti, portali. Se non hai nulla, va bene: partiamo dalla visita.
          </p>
          <ul class="fv-prep-list">
            {preparationItems.map((item) => <li>{item}</li>)}
          </ul>
        </div>
        <figure class="fv-split-image">
          <img src="/assets/original/2021/02/HOME.jpg" alt="Dottor Stefano Granata e Dottoressa Chiara Federzoni" loading="eager" />
        </figure>
      </div>
    </section>

    <section class="fv-final" aria-labelledby="final-title">
      <div class="fv-wrap fv-final-inner">
        <div>
          <p class="fv-kicker">Prenota la tua prima visita</p>
          <h2 id="final-title" aria-label="Hai un dubbio? Iniziamo da lì">
            <span>Hai un dubbio?</span>
            <span>Iniziamo da lì</span>
          </h2>
          <p>
            Se hai dolore, un controllo rimandato, gengive che sanguinano,
            denti storti, impianti o estetica da valutare, raccontaci cosa sta succedendo.
            Ti richiamiamo noi, ascoltiamo il motivo della richiesta e troviamo
            l’appuntamento più comodo tra Modena e Reggio Emilia.
          </p>
        </div>
        <div class="fv-final-actions">
          <a class="fv-button fv-button-primary" href={firstVisitFormPath}>Prenota visita <span aria-hidden="true">↗</span></a>
          <div class="fv-office-list" aria-label="Sedi dello studio">
            {offices.map((office) => (
              <a href={office.mapHref} target="_blank" rel="noreferrer">
                <strong>{office.city}</strong>
                <span>{office.address}</span>
              </a>
            ))}
          </div>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .first-visit-page {
    --fv-blue: var(--brand-cta);
    --fv-blue-bright: var(--brand-blue);
    --fv-blue-soft: var(--soft-blue-bg);
    --fv-ink: var(--brand-primary-dark);
    --fv-muted: var(--muted-text);
    --fv-line: rgba(var(--brand-blue-rgb), 0.2);
    --fv-description-size: clamp(1.05rem, 1.15vw, 1.12rem);
    background: var(--surface-white);
    color: var(--fv-ink);
    font-family: "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    letter-spacing: 0;
  }

  .first-visit-page :global(a) {
    color: inherit;
  }

  .fv-wrap {
    width: min(1510px, calc(100% - 30px));
    margin: 0 auto;
  }

  .fv-hero {
    background: var(--surface-white);
    color: var(--surface-white);
  }

  .fv-hero-panel {
    min-height: calc(65svh - var(--nav-height));
    display: grid;
    background: var(--blue-dark);
  }

  .fv-hero-inner {
    min-height: inherit;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 150px 0 42px;
  }

  .fv-breadcrumb {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: center;
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.84rem;
    font-weight: 800;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .fv-breadcrumb a {
    text-decoration: none;
  }

  .fv-breadcrumb a:hover {
    color: var(--surface-white);
  }

  .fv-hero-copy {
    width: min(100%, 1360px);
    margin: 0 auto;
    text-align: center;
  }

  .fv-kicker {
    margin: 0 0 14px;
    color: var(--fv-blue-bright);
    font-size: 0.88rem;
    font-weight: 400;
    letter-spacing: 0;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .fv-hero .fv-kicker,
  .fv-final .fv-kicker {
    color: var(--aqua);
  }

  .fv-hero .fv-kicker {
    margin-bottom: 24px;
    font-size: 1rem;
  }

  .fv-hero h1 {
    max-width: none;
    margin: 0;
    color: var(--surface-white);
    font-family: "Inter", ui-sans-serif, system-ui, sans-serif;
    font-size: clamp(4.8rem, 10.6vw, 10.75rem);
    font-weight: 400;
    letter-spacing: 0;
    line-height: 0.88;
    text-transform: uppercase;
  }

  .fv-hero-copy > p:not(.fv-kicker) {
    display: none;
    max-width: 880px;
    margin: 34px auto 0;
    color: var(--aqua);
    font-size: clamp(1.2rem, 1.75vw, 1.65rem);
    font-weight: 400;
    line-height: 1.25;
  }

  .fv-hero-media {
    height: clamp(360px, 42vw, 620px);
    margin: 0;
    overflow: hidden;
    background: var(--border);
  }

  .fv-hero-media picture {
    width: 100%;
    height: 100%;
    display: block;
  }

  .fv-hero-media img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    object-position: center 42%;
  }

  .fv-actions,
  .fv-final-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    align-items: center;
  }

  .fv-actions {
    margin-top: 34px;
  }

  .fv-button {
    min-width: min(100%, 286px);
    min-height: 58px;
    display: inline-flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    border: 1px solid rgba(var(--brand-primary-dark-rgb), 0.52);
    border-radius: 8px;
    padding: 0 24px;
    background: transparent;
    color: var(--fv-blue);
    font-size: 1rem;
    font-weight: 500;
    letter-spacing: 0;
    line-height: 1;
    text-align: center;
    text-decoration: none;
    text-transform: uppercase;
    transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease, transform 160ms ease;
  }

  .fv-button > span[aria-hidden="true"],
  .fv-inline-link > span[aria-hidden="true"] {
    font-size: 1.45rem;
    line-height: 1;
  }

  .fv-button:hover {
    border-color: var(--fv-blue);
    background: rgba(var(--brand-primary-dark-rgb), 0.06);
    color: var(--fv-blue);
    transform: translateY(-1px);
  }

  .fv-button-primary {
    border-color: var(--fv-blue);
    background: var(--fv-blue);
    color: var(--surface-white);
  }

  .fv-button-primary:hover {
    border-color: var(--brand-cta-hover);
    background: var(--brand-cta-hover);
    color: var(--surface-white);
  }

  .fv-button-secondary {
    border-color: rgba(255, 255, 255, 0.55);
    background: rgba(255, 255, 255, 0.1);
    color: var(--surface-white);
  }

  .fv-button-secondary:hover {
    border-color: var(--surface-white);
    background: var(--surface-white);
    color: var(--fv-blue);
  }

  .fv-steps-section,
  .fv-evaluation,
  .fv-reasons-section {
    padding: 92px 0;
  }

  .fv-trust-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(0, 1fr);
    gap: 70px;
    align-items: start;
  }

  .fv-section-head h2,
  .fv-split-copy h2,
  .fv-trust-grid h2,
  .fv-final h2 {
    margin: 0;
    color: var(--fv-blue);
    font-family: "Inter", ui-sans-serif, system-ui, sans-serif;
    font-size: 3.75rem;
    font-weight: 550;
    letter-spacing: 0;
    line-height: 1;
  }

  .fv-split-copy p,
  .fv-trust-copy p,
  .fv-section-head > p,
  .fv-final p {
    margin: 0;
    color: var(--fv-muted);
    font-size: var(--fv-description-size);
    line-height: 1.62;
  }

  .fv-steps-section {
    background: var(--surface-white);
  }

  .fv-section-head {
    display: grid;
    grid-template-columns: minmax(0, 0.65fr) minmax(420px, 0.8fr);
    gap: 42px;
    align-items: end;
    margin-bottom: 42px;
  }

  .fv-section-head-right h2 {
    justify-self: end;
    text-align: right;
  }

  .fv-section-head-centered {
    max-width: 880px;
    display: block;
    margin-right: auto;
    margin-left: auto;
    text-align: center;
  }

  .fv-section-head-centered h2 {
    margin-top: 0;
  }

  .fv-section-head-centered > p {
    margin-top: 22px;
  }

  .fv-step-rail {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: clamp(18px, 1.8vw, 34px);
    align-items: start;
    background: transparent;
  }

  .fv-step-card {
    min-height: clamp(330px, 22vw, 400px);
    display: grid;
    grid-template-rows: auto 1fr;
    gap: clamp(20px, 2.4vw, 34px);
    border-radius: 8px;
    padding: clamp(28px, 2.4vw, 42px);
    background: var(--fv-step-bg, rgba(var(--brand-blue-rgb), 0.1));
    color: var(--fv-ink);
  }

  .fv-step-card:nth-child(2n) {
    --fv-step-bg: rgba(var(--brand-cta-rgb), 0.08);
  }

  .fv-step-number {
    color: var(--fv-blue);
    font-size: clamp(4.8rem, 5.9vw, 7.2rem);
    font-weight: 400;
    line-height: 0.84;
  }

  .fv-step-body {
    display: grid;
    align-self: end;
    gap: 18px;
  }

  .fv-step-card h3 {
    max-width: 290px;
    margin: 0;
    color: var(--fv-blue);
    font-family: "Inter", ui-sans-serif, system-ui, sans-serif;
    font-size: clamp(1.55rem, 1.75vw, 2.2rem);
    font-weight: 500;
    letter-spacing: 0;
    line-height: 1.03;
    text-transform: none;
  }

  .fv-step-body p,
  .fv-evaluation-card p,
  .fv-reason-card p {
    margin: 0;
    color: var(--muted-text);
    font-size: var(--fv-description-size);
    line-height: 1.45;
  }

  .fv-step-body p {
    max-width: 420px;
    color: var(--fv-muted);
    font-weight: 400;
    line-height: 1.45;
  }

  .fv-step-cta {
    display: flex;
    justify-content: center;
    margin-top: clamp(42px, 5vw, 72px);
  }

  .fv-step-cta .fv-button {
    min-width: min(100%, 304px);
    border-color: var(--fv-blue);
    background: var(--surface-white);
    color: var(--fv-blue);
  }

  .fv-step-cta .fv-button:hover {
    border-color: var(--brand-cta-hover);
    background: var(--surface-white);
    color: var(--brand-cta-hover);
  }

  .fv-split {
    padding: 92px 0;
    background: var(--surface-white);
  }

  .fv-split-soft {
    background: var(--fv-blue-soft);
  }

  .fv-split-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 0.92fr);
    gap: 86px;
    align-items: center;
  }

  .fv-split-grid-reverse {
    grid-template-columns: minmax(0, 0.92fr) minmax(0, 1fr);
  }

  .fv-split-image {
    margin: 0;
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 8px;
  }

  .fv-split-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .fv-split-copy {
    max-width: 640px;
  }

  .fv-split-copy p {
    margin-top: 22px;
  }

  .fv-split-soft .fv-kicker {
    margin: 0 0 28px;
  }

  .fv-split-grid-reverse .fv-kicker {
    margin: 0 0 28px;
  }

  #diagnosis-title,
  #steps-title,
  #evaluation-title,
  #trust-title,
  #reasons-title,
  #final-title {
    letter-spacing: -0.035em;
  }

  .fv-inline-link {
    min-height: 52px;
    display: inline-flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    margin-top: 30px;
    border: 1px solid rgba(var(--brand-blue-rgb), 0.42);
    border-radius: 8px;
    padding: 0 20px;
    color: var(--fv-blue-bright);
    font-family: "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    font-size: 16px;
    font-weight: 500;
    letter-spacing: -0.2px;
    line-height: 16px;
    text-decoration: none;
    text-transform: uppercase;
    word-spacing: 0;
  }

  .fv-inline-link:hover {
    background: rgba(var(--brand-blue-rgb), 0.1);
    color: var(--fv-blue);
  }

  .fv-evaluation {
    background: var(--surface-white);
  }

  .fv-evaluation .fv-section-head .fv-kicker {
    margin: 0 0 28px;
  }

  .fv-evaluation-grid,
  .fv-reasons-grid {
    display: grid;
    gap: 1px;
    margin-top: 44px;
    background: var(--fv-line);
  }

  .fv-evaluation-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .fv-reasons-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .fv-evaluation-card,
  .fv-reason-card {
    min-height: 250px;
    padding: 28px;
    background: var(--surface-white);
  }

  .fv-evaluation-card {
    min-height: 290px;
    display: grid;
    grid-template-rows: auto 1fr;
    gap: 28px;
  }

  .fv-reason-card {
    min-height: 286px;
    display: grid;
    grid-template-rows: auto 1fr;
    gap: 28px;
  }

  .fv-evaluation-icon,
  .fv-reason-icon {
    width: 58px;
    height: 58px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
    background: rgba(var(--brand-blue-rgb), 0.12);
    box-shadow: inset 0 0 0 1px rgba(var(--brand-blue-rgb), 0.22);
    color: var(--fv-blue);
  }

  .fv-evaluation-card:nth-child(2n) .fv-evaluation-icon,
  .fv-reason-card:nth-child(2n) .fv-reason-icon {
    background: rgba(var(--brand-cta-rgb), 0.1);
    box-shadow: inset 0 0 0 1px rgba(var(--brand-cta-rgb), 0.2);
    color: var(--brand-cta-hover);
  }

  .fv-evaluation-icon svg,
  .fv-reason-icon svg {
    width: 36px;
    height: 36px;
    display: block;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 2;
  }

  .fv-evaluation-copy,
  .fv-reason-copy {
    align-self: end;
    display: grid;
    gap: 20px;
  }

  .fv-evaluation-card strong,
  .fv-reason-card h3 {
    margin: 0;
    color: var(--fv-blue);
    font-family: "Inter", ui-sans-serif, system-ui, sans-serif;
    font-size: 1.55rem;
    font-weight: 650;
    letter-spacing: 0;
    line-height: 1.05;
  }

  .fv-trust-band {
    padding: 90px 0;
    background: var(--brand-primary-dark);
    color: var(--surface-white);
  }

  .fv-trust-band .fv-kicker,
  .fv-trust-band h2,
  .fv-trust-copy p {
    color: var(--surface-white);
  }

  .fv-trust-copy {
    display: grid;
    gap: 22px;
  }

  .fv-trust-band .fv-trust-cta {
    width: fit-content;
    margin-top: 10px;
    border-color: rgba(var(--surface-white-rgb), 0.72);
    background: var(--surface-white);
    color: var(--fv-blue);
  }

  .fv-trust-band .fv-trust-cta:hover {
    border-color: var(--surface-white);
    background: var(--fv-blue-soft);
    color: var(--brand-cta-hover);
  }

  .fv-reasons-section {
    background: var(--fv-blue-soft);
  }

  .fv-reason-card {
    background: var(--surface-white);
  }

  .fv-prep-list {
    display: grid;
    gap: 14px;
    margin: 28px 0 0;
    padding: 0;
    list-style: none;
  }

  .fv-prep-list li {
    position: relative;
    padding-left: 26px;
    color: var(--fv-muted);
    font-size: var(--fv-description-size);
    line-height: 1.5;
  }

  .fv-prep-list li::before {
    content: "";
    position: absolute;
    top: 0.62em;
    left: 0;
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: var(--fv-blue-bright);
  }

  .fv-final {
    padding: 86px 0;
    background: var(--blue-dark);
    color: var(--soft-blue-bg);
  }

  .fv-final-inner {
    display: grid;
    grid-template-columns: minmax(0, 0.98fr) minmax(340px, 0.62fr);
    gap: 64px;
    align-items: center;
  }

  .fv-final h2 {
    color: var(--aqua);
  }

  .fv-final h2 span {
    display: block;
  }

  .fv-final p {
    max-width: 780px;
    margin-top: 22px;
    color: rgba(255, 255, 255, 0.78);
  }

  .fv-final .fv-kicker {
    margin: 0 0 28px;
    color: var(--surface-white);
  }

  .fv-final .fv-button-primary {
    border-color: var(--soft-blue-bg);
    background: var(--soft-blue-bg);
    color: var(--blue-dark);
  }

  .fv-final .fv-button-primary:hover {
    border-color: var(--aqua);
    background: var(--aqua);
    color: var(--blue-dark);
  }

  .fv-final-actions {
    align-items: stretch;
    justify-content: flex-start;
  }

  .fv-office-list {
    display: grid;
    gap: 10px;
    min-width: min(100%, 360px);
  }

  .fv-office-list a {
    display: grid;
    gap: 4px;
    border: 1px solid rgba(var(--brand-blue-rgb), 0.28);
    border-radius: 8px;
    padding: 14px 16px;
    background: transparent;
    color: var(--soft-blue-bg);
    text-decoration: none;
  }

  .fv-office-list a:hover {
    background: rgba(var(--brand-blue-rgb), 0.12);
  }

  .fv-office-list strong {
    font-size: 0.98rem;
    line-height: 1.2;
  }

  .fv-office-list span {
    color: rgba(255, 255, 255, 0.78);
    font-size: 0.9rem;
    line-height: 1.35;
  }

  @media (max-width: 1400px) {
    .fv-step-rail {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 1120px) {
    .fv-hero-panel {
      min-height: 620px;
    }

    .fv-hero h1 {
      font-size: clamp(4.6rem, 11vw, 7rem);
    }

    .fv-trust-grid,
    .fv-section-head,
    .fv-split-grid,
    .fv-split-grid-reverse,
    .fv-final-inner {
      grid-template-columns: 1fr;
      gap: 42px;
    }

    .fv-evaluation-grid,
    .fv-reasons-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .fv-split-image {
      max-height: 620px;
    }
  }

  @media (max-width: 720px) {
    .fv-wrap {
      width: min(100% - 22px, 1510px);
    }

    .first-visit-page .fv-kicker {
      color: var(--brand-blue);
    }

    .fv-steps-section .fv-kicker {
      text-align: center;
    }

    .fv-steps-section .fv-section-head h2 {
      justify-self: center;
      text-align: center;
    }

    .fv-evaluation .fv-section-head,
    .fv-evaluation .fv-section-head h2,
    .fv-evaluation .fv-section-head > p,
    .fv-evaluation .fv-evaluation-card,
    .fv-evaluation .fv-evaluation-copy,
    .fv-evaluation .fv-evaluation-copy strong,
    .fv-evaluation .fv-evaluation-copy p {
      text-align: center;
    }

    .fv-evaluation .fv-evaluation-icon {
      justify-self: center;
    }

    .fv-reasons-section .fv-section-head,
    .fv-reasons-section .fv-section-head h2,
    .fv-reasons-section .fv-section-head > p,
    .fv-reasons-section .fv-reason-card,
    .fv-reasons-section .fv-reason-copy,
    .fv-reasons-section .fv-reason-copy h3,
    .fv-reasons-section .fv-reason-copy p {
      text-align: center;
    }

    .fv-reasons-section .fv-section-head h2,
    .fv-reasons-section .fv-reason-icon {
      justify-self: center;
    }

    .fv-hero-panel {
      min-height: 280px;
    }

    .fv-hero-inner {
      padding: 72px 0 40px;
    }

    .fv-hero-copy {
      padding-top: 0;
    }

    .fv-hero .fv-kicker {
      display: none;
    }

    .fv-hero h1 {
      font-size: 3.35rem;
      line-height: 0.92;
    }

    .fv-hero-copy > p:not(.fv-kicker) {
      display: none;
    }

    .fv-hero-media {
      height: 310px;
    }

    .fv-steps-section,
    .fv-evaluation,
    .fv-reasons-section,
    .fv-split,
    .fv-trust-band,
    .fv-final {
      padding: 62px 0;
    }

    .fv-section-head h2,
    .fv-split-copy h2,
    .fv-trust-grid h2,
    .fv-final h2 {
      font-size: 2.35rem;
      line-height: 1.04;
    }

    .fv-final h2 {
      color: var(--surface-white);
    }

    .fv-section-head {
      margin-bottom: 30px;
    }

    .fv-split-soft .fv-kicker {
      margin-bottom: 20px;
    }

    .fv-split-grid-reverse .fv-kicker {
      margin-bottom: 20px;
    }

    .fv-evaluation .fv-section-head .fv-kicker {
      margin-bottom: 20px;
    }

    .fv-final .fv-kicker {
      margin-bottom: 20px;
    }

    .fv-step-rail,
    .fv-evaluation-grid,
    .fv-reasons-grid {
      grid-template-columns: 1fr;
    }

    .fv-step-card,
    .fv-evaluation-card,
    .fv-reason-card {
      min-height: auto;
      padding: 24px;
    }

    .fv-step-card h3 {
      font-size: 1.7rem;
    }

    .fv-split-grid-reverse .fv-split-image {
      order: -1;
    }

    .fv-actions,
    .fv-final-actions {
      align-items: stretch;
    }

    .fv-button {
      width: 100%;
    }

    /* Ritmo tipografico mobile allineato alla home. */
    .fv-wrap {
      width: min(100% - 30px, 1510px);
    }

    .fv-steps-section,
    .fv-evaluation,
    .fv-reasons-section,
    .fv-split,
    .fv-trust-band,
    .fv-final {
      padding: 58px 0;
    }

    .first-visit-page .fv-kicker {
      font-size: 14px;
      font-weight: 600;
      letter-spacing: -0.00308px;
      line-height: 18.0008px;
    }

    .fv-section-head {
      gap: 20px;
      margin-bottom: 40px;
    }

    .fv-section-head .fv-kicker {
      margin: 0;
    }

    .fv-evaluation .fv-section-head .fv-kicker,
    .fv-split-copy > .fv-kicker,
    .fv-trust-grid > div > .fv-kicker,
    .fv-final .fv-kicker {
      margin: 0 0 20px;
    }

    #first-visit-title {
      font-size: 40px;
      font-weight: 600;
      letter-spacing: normal;
      line-height: 44px;
    }

    #diagnosis-title,
    #steps-title,
    #evaluation-title,
    #trust-title,
    #reasons-title,
    #prepare-title,
    #final-title {
      font-size: 40px;
      font-weight: 600;
      letter-spacing: normal;
      line-height: 41px;
    }

    .fv-split-copy > p:not(.fv-kicker),
    .fv-section-head > p:not(.fv-kicker),
    .fv-trust-copy p,
    .fv-final h2 + p,
    .fv-step-body p,
    .fv-evaluation-card p,
    .fv-reason-card p,
    .fv-prep-list li {
      font-size: 17px;
      font-weight: 400;
      letter-spacing: -0.374px;
      line-height: 25px;
    }

    .fv-split-copy > p:not(.fv-kicker),
    .fv-section-head-centered > p:not(.fv-kicker),
    .fv-final h2 + p {
      margin-top: 20px;
    }

    .fv-step-body,
    .fv-evaluation-copy,
    .fv-reason-copy {
      gap: 14px;
    }

    .fv-step-card h3,
    .fv-evaluation-card strong,
    .fv-reason-card h3 {
      font-size: 19px;
      font-weight: 600;
      letter-spacing: 0.228px;
      line-height: 23px;
    }

    .fv-evaluation-grid,
    .fv-reasons-grid {
      margin-top: 0;
    }

    .fv-split-grid,
    .fv-split-grid-reverse {
      gap: 32px;
    }

    .fv-trust-grid,
    .fv-final-inner {
      gap: 40px;
    }

    .fv-trust-copy {
      gap: 20px;
    }

    .fv-trust-band .fv-trust-cta {
      margin-top: 0;
    }

    .fv-prep-list {
      margin-top: 20px;
    }

    .fv-step-cta {
      margin-top: 34px;
    }

    .fv-button,
    .fv-inline-link {
      box-sizing: border-box;
      font-size: 17px;
      font-weight: 400;
      letter-spacing: -0.374px;
      line-height: 20.0002px;
    }

    .fv-inline-link {
      width: 100%;
      min-height: 58px;
      margin-top: 24px;
    }

    .fv-button > span[aria-hidden="true"],
    .fv-inline-link > span[aria-hidden="true"] {
      width: 24px;
      height: 24px;
      display: inline-grid;
      flex: 0 0 24px;
      place-items: center;
      font-family: "Inter", ui-sans-serif, system-ui, sans-serif !important;
      font-size: 23.2px;
      font-weight: 400;
      letter-spacing: normal;
      line-height: 23.2px;
    }

    .fv-office-list strong,
    .fv-office-list span {
      font-size: 14px;
      line-height: 18.0008px;
    }

    .fv-office-list strong {
      font-weight: 600;
      letter-spacing: -0.00308px;
    }

    .fv-office-list span {
      font-weight: 400;
      letter-spacing: -0.224px;
    }
  }
</style>
